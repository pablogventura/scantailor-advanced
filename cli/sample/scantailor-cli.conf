# Configuración por defecto para scantailor-cli (todo en automático)
# Los argumentos de la línea de comandos tienen prioridad sobre este archivo.
#
# project = ruta al archivo .ScanTailor (relativa a este config o absoluta)
# threads = número de hilos para el batch (opcional)
# output-dir = directorio de salida override (opcional)

project=project.ScanTailor
threads=2
